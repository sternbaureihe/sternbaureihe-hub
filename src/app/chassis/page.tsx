import Nav from '@/components/Nav'; import Footer from '@/components/Footer'; import { chassisData } from '@/data/chassis'; import styles from './page.module.css';
export default function ChassisPage() {
  const byClass = chassisData.reduce((acc, c) => { if (!acc[c.class]) acc[c.class] = []; acc[c.class].push(c); return acc }, {} as Record<string, typeof chassisData>)
  return (<><Nav /><div className={styles.header}><div className={styles.eyebrow}>Chassis Index</div><h1 className={styles.headline}>100+ chassis documented.</h1><p className={styles.body}>Factory specifications, technical documentation, and primary source data for every Mercedes-Benz chassis code.</p></div>{Object.entries(byClass).sort().map(([cls, items]) => (<div key={cls}><div className={styles.classHeader}><span className={styles.classTitle}>{cls}</span></div><div className={styles.grid}>{items.map(c => (<a key={c.code} href={'https://'+c.domain} className={styles.card} target='_blank' rel='noopener'><div className={styles.cardThumb}><span className={styles.cardCode}>{c.code}</span></div><div className={styles.cardInfo}><span className={styles.cardName}>{c.code}</span><span className={styles.cardSub}>{c.name}</span><span className={styles.cardYears}>{c.years}</span><div className={styles.cardBodies}>{c.bodies.slice(0,3).map(b => <span key={b} className={styles.cardTag}>{b}</span>)}</div></div></a>))}</div></div>))}<Footer /></>)
}
